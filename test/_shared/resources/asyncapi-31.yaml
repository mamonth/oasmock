asyncapi: 3.1.0
info:
  title: Webhook Events
  version: 1.0.0
channels:
  signedUp:
    address: user/signedup
    messages:
      signedUpMsg:
        examples:
          - payload:
              id: 1
operations:
  receiveSignedUp:
    action: receive
    channel:
      $ref: '#/channels/signedUp'