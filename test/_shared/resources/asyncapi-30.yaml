asyncapi: 3.0.0
info:
  title: User Events
  version: 1.0.0
channels:
  userSignedUp:
    address: user/signedup
    messages:
      auserSignedUp:
        examples:
          - payload:
              id: "{$request.path.id}"
operations:
  receiveUserSignedUp:
    action: receive
    channel:
      $ref: '#/channels/userSignedUp'